/**
 * The `SchemeDetectionRule` specifies how a data scheme should be detected.
 * Currently, two methods are supported:
 * - file name matches a pattern (either a specific file name, or an extension)
 * - the file contains a certain string, e.g. when a scheme is specified in the
 *   header of an XML file
 */
export class SchemeDetectionRule {}
